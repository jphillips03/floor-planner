package floor.planner.constants;

public enum Orientation {
    EAST_WEST,
    WEST_EAST,
    NORTH_SOUTH,
    SOUTH_NORTH,
    COLUMN // not an orientation per se, but they are a special kind of wall
}
